# Cuenta de servicio de Firebase (el MISMO valor que ya usa el portal interno).
FIREBASE_SERVICE_ACCOUNT={"type":"service_account", ...}

# Administración de accesos (/admin). Solo María.
SD_ADMIN_PASSWORD=
# Cualquier cadena larga y aleatoria: es el valor de la cookie de administración.
SD_ADMIN_TOKEN=
